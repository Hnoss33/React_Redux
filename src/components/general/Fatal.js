import React from 'react';

const Fatal = (props) => (
    <h2 className='center Rojo'>
{props.mensaje} 
    </h2>
);
//este faltal error aparece cuando el recuest que se realiza al URL  esta incorrecto o el server no responde 

export default Fatal;

